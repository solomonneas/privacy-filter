#!/bin/bash
# Single verification entrypoint. AGENTS.md's Definition of Done defers to
# this file, so the gate lives in exactly one place.
# Runs the unit tests only; they never load the model, so no checkpoint or
# weights download is ever triggered.
set -euo pipefail
cd "$(dirname "$0")/.."

python3 -m unittest discover tests
